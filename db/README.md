# Database Migration Guide - Normalized Recipe Schema

## 📋 Обзор

Безопасная миграция для добавления нормализованной модели рецептов и ингредиентов в существующую Supabase базу данных.

**Ключевой принцип**: ТОЛЬКО additive changes. Никаких destructive операций (DROP, RENAME, TRUNCATE).

## 🗂️ Структура новых таблиц

### `recipes`
- Основная таблица рецептов
- Хранит URL, название, базовое количество порций

### `ingredients`
- Справочник ингредиентов
- Нормализованные названия ингредиентов

### `units`
- Справочник единиц измерения
- Коды: g, ml, pcs, tsp, tbsp, pinch, etc.

### `recipe_ingredients`
- Связь многие-ко-многим между рецептами и ингредиентами
- Хранит количество, единицы, порядок отображения

## 🚀 Пошаговая инструкция запуска

### Фаза 1: Диагностика существующей схемы

```sql
-- В Supabase SQL Editor запустите:
-- db/introspect.sql
```

**Цель**: Понять текущую структуру БД, проверить конфликты имён таблиц, статус RLS.

**Что проверить в результатах**:
- ✅ Нет конфликтующих таблиц с именами: `recipes`, `ingredients`, `units`, `recipe_ingredients`
- ✅ Если есть конфликты — используйте префикс `v2_` (например `v2_recipes`)
- ✅ Статус RLS для существующих таблиц
- ✅ Существующие Foreign Keys и индексы

**Если есть конфликты имён**:
1. Откройте `db/migrate_add_normalized_schema.sql`
2. Замените все упоминания `recipes` на `v2_recipes` (и аналогично для других таблиц)
3. Обновите соответственно все остальные файлы

---

### Фаза 2: Создание новых таблиц

```sql
-- В Supabase SQL Editor запустите:
-- db/migrate_add_normalized_schema.sql
```

**Что происходит**:
- ✅ Создаются 4 таблицы с IF NOT EXISTS
- ✅ Добавляются все индексы
- ✅ Добавляются Foreign Keys безопасно
- ✅ Создаётся триггер для auto-update `updated_at`
- ✅ Никаких изменений в существующих таблицах

**Ожидаемый результат**:
```
MIGRATION COMPLETED | recipes_exists: 1 | ingredients_exists: 1 | units_exists: 1 | recipe_ingredients_exists: 1
```

**В случае ошибок**:
- Скрипт идемпотентен — можно запускать повторно
- Проверьте output introspect.sql на конфликты
- Проверьте права пользователя на CREATE TABLE

---

### Фаза 3: Заполнение справочника единиц

```sql
-- В Supabase SQL Editor запустите:
-- db/seed_units.sql
```

**Что происходит**:
- ✅ Добавляются базовые единицы измерения (г, мл, шт, ч.л., ст.л., щепотка)
- ✅ Добавляются дополнительные единицы (кг, л, стакан, и т.д.)
- ✅ Использует ON CONFLICT DO NOTHING — безопасно для повторного запуска

**Ожидаемый результат**:
```
total_units: 19
available_codes: {bag,bunch,can,clove,cup,g,handful,kg,l,lb,ml,oz,package,pcs,pinch,slice,tbsp,to_taste,tsp}
```

---

### Фаза 4: Настройка RLS (опционально)

```sql
-- В Supabase SQL Editor запустите:
-- db/rls_safe_defaults.sql
```

**Что происходит**:
- ✅ Проверяется статус RLS на новых таблицах
- ✅ Если RLS включён — добавляются минимальные политики SELECT для authenticated
- ✅ Если RLS выключен — ничего не делается
- ✅ НЕ добавляются политики INSERT/UPDATE/DELETE (для безопасности)

**Рекомендации**:
- Для production: добавьте write-политики в зависимости от бизнес-логики
- Для development: можно оставить RLS выключенным

---

### Фаза 5: Проверка миграции

```sql
-- В Supabase SQL Editor запустите:
-- db/smoke_check.sql
```

**Что проверяется**:
- ✅ Существование всех таблиц
- ✅ Наличие записей в `units` (минимум 6)
- ✅ Все индексы созданы
- ✅ Foreign Keys настроены
- ✅ Уникальные ограничения на месте
- ✅ RLS статус
- ✅ Триггеры

**Ожидаемый результат**:
```
✓ ALL CHECKS PASSED
Migration completed successfully!
```

---

### Фаза 6: VIEWs для совместимости (опционально)

```sql
-- Если нужна обратная совместимость со старым форматом:
-- views/compat_recipe_view.sql
```

**Когда использовать**:
- У вас есть старая таблица рецептов с другим форматом
- Нужно постепенно мигрировать приложение
- Хотите использовать оба формата параллельно

**Создаваемые VIEWs**:
1. `compat_recipe_view` — рецепты с ингредиентами в JSONB (старый формат)
2. `recipe_with_ingredients_array` — рецепты с массивом структурированных ингредиентов
3. `recipe_ingredient_search` — плоский VIEW для поиска по ингредиентам

---

## 📊 Следующие шаги

### 1. Импорт данных рецептов
```sql
-- Пример INSERT нового рецепта:
BEGIN;

-- Создать рецепт
INSERT INTO recipes (url, name, base_servings)
VALUES ('https://example.com/recipe/1', 'Паста Карбонара', 2)
RETURNING id;  -- Допустим получили id = 1

-- Добавить ингредиенты
INSERT INTO ingredients (name) 
VALUES ('Спагетти'), ('Бекон'), ('Яйца'), ('Пармезан')
ON CONFLICT (name) DO NOTHING;

-- Связать рецепт с ингредиентами
INSERT INTO recipe_ingredients (recipe_id, ingredient_id, amount, unit_id, sort_order)
VALUES 
    (1, (SELECT id FROM ingredients WHERE name = 'Спагетти'), 200, (SELECT id FROM units WHERE code = 'g'), 1),
    (1, (SELECT id FROM ingredients WHERE name = 'Бекон'), 100, (SELECT id FROM units WHERE code = 'g'), 2),
    (1, (SELECT id FROM ingredients WHERE name = 'Яйца'), 2, (SELECT id FROM units WHERE code = 'pcs'), 3),
    (1, (SELECT id FROM ingredients WHERE name = 'Пармезан'), 50, (SELECT id FROM units WHERE code = 'g'), 4);

COMMIT;
```

### 2. Обновление кода приложения

**Пример запроса рецепта с ингредиентами**:
```sql
SELECT 
    r.*,
    json_agg(
        json_build_object(
            'ingredient', i.name,
            'amount', ri.amount,
            'unit', u.name_ru,
            'is_optional', ri.is_optional
        ) ORDER BY ri.sort_order
    ) as ingredients
FROM recipes r
LEFT JOIN recipe_ingredients ri ON ri.recipe_id = r.id
LEFT JOIN ingredients i ON i.id = ri.ingredient_id
LEFT JOIN units u ON u.id = ri.unit_id
WHERE r.id = $1
GROUP BY r.id;
```

### 3. Добавление RLS политик для записи (если нужно)

```sql
-- Пример: разрешить authenticated пользователям создавать рецепты
CREATE POLICY recipes_insert_authenticated
ON recipes FOR INSERT
TO authenticated
WITH CHECK (true);

-- Пример: разрешить пользователям редактировать только свои рецепты
-- (нужно добавить колонку user_id в recipes)
CREATE POLICY recipes_update_own
ON recipes FOR UPDATE
TO authenticated
USING (user_id = auth.uid())
WITH CHECK (user_id = auth.uid());
```

---

## ⚠️ Важные замечания

### Безопасность миграции
- ✅ Все скрипты идемпотентны — можно запускать повторно
- ✅ Никаких DROP/RENAME/TRUNCATE
- ✅ Существующие таблицы и данные НЕ затрагиваются
- ✅ Можно откатить, просто удалив новые таблицы (но это не рекомендуется)

### Откат миграции (emergency only)
```sql
-- ВНИМАНИЕ: Удаление таблиц и всех данных!
-- Используйте только если что-то пошло не так
DROP TABLE IF EXISTS recipe_ingredients CASCADE;
DROP TABLE IF EXISTS recipes CASCADE;
DROP TABLE IF EXISTS ingredients CASCADE;
DROP TABLE IF EXISTS units CASCADE;
DROP VIEW IF EXISTS compat_recipe_view CASCADE;
DROP VIEW IF EXISTS recipe_with_ingredients_array CASCADE;
DROP VIEW IF EXISTS recipe_ingredient_search CASCADE;
```

### Производительность
- Индексы созданы на всех ключевых полях
- JOIN запросы оптимизированы для чтения рецептов
- Для больших объёмов данных (>100k рецептов) рассмотрите:
  - Партицирование таблицы `recipe_ingredients`
  - Материализованные VIEWs для часто используемых запросов
  - Полнотекстовый поиск (pg_trgm уже настроен для ingredients)

---

## 📞 Поддержка

При возникновении проблем:
1. Запустите `db/smoke_check.sql` и проверьте результаты
2. Проверьте логи Supabase на наличие ошибок
3. Убедитесь, что пользователь имеет права на CREATE TABLE, CREATE INDEX
4. Проверьте конфликты имён через `db/introspect.sql`

---

## ✅ Checklist финального запуска

- [ ] Бэкап текущей БД создан
- [ ] Запущен и проверен `db/introspect.sql`
- [ ] Проверены конфликты имён таблиц
- [ ] Запущен `db/migrate_add_normalized_schema.sql` — успешно
- [ ] Запущен `db/seed_units.sql` — units заполнены
- [ ] Запущен `db/rls_safe_defaults.sql` — политики настроены (если нужно)
- [ ] Запущен `db/smoke_check.sql` — все проверки пройдены ✓
- [ ] VIEWs созданы (если нужны)
- [ ] Тестовые данные загружены
- [ ] Приложение обновлено для работы с новой схемой
- [ ] Monitoring настроен для отслеживания производительности

**Миграция завершена! 🎉**
